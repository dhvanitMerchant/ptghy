import React from 'react'

function PackageLinks({singlePackageLinks}) {
    return (
            <div class="container">
                    {singlePackageLinks.packageName}
            </div>
    )
}

export default PackageLinks;